---
const paymentMethods = [
  {
    title: 'Convenios para pagos físicos',
    items: [
      {
        href: '',
        name: 'Efecty',
        convenio: '112426',
        image: '/images/efecty.png', // Ajuste o caminho da imagem
      },
      {
        href: '',
        name: 'Gana',
        convenio: '384',
        image: '/images/gana.png', // Ajuste o caminho da imagem
      },
      {
        href: '',
        name: 'Banco Caja Social',
        convenio: 'VELONET SAS',
        image: '/images/caja_social.png', // Ajuste o caminho da imagem
      },
    ],
  },
  {
    title: 'Pagos digitales a un clic',
    items: [
      {
        href: 'https://www.psepagos.co/PSEHostingUI/DatabaseTicketOffice.aspx?ID=10932',
        name: 'PSE',
        convenio: '',
        image: '/images/pse.png', // Ajuste o caminho da imagem
      },
      {
        href: 'https://www.mipagoamigo.com/MPA_WebSite/ServicePayments',
        name: 'MiPagoAmigo',
        convenio: '',
        image: '/images/mipago.png', // Ajuste o caminho da imagem
      },
    ],
  },
];

import Layout from '../layouts/Layout.astro';
---

<Layout title="Mi pago">
    <section class="bg-roxo text-white py-16 px-8 flex flex-col items-center">
        <img src="/images/velonet-logo.svg" alt="Velonet Logo" class="mb-8 h-20" />
      
        <p class="text-center text-lg mb-8 max-w-2xl">
          Tu pago es importante para que juntos sigamos trabajando para reducir la brecha digital en Colombia. Realiza tu pago de <strong>servicios fijos hogar</strong> en puntos GANA o EFECTY. También lo puedes hacer en línea en medios autorizados.
        </p>
      
        <div class="grid grid-cols-1 md:grid-cols-2 gap-8 max-w-4xl">
          <!-- Pagos físicos -->
          <div class="bg-white text-black rounded-lg p-8 shadow-lg">
            <h3 class="text-xl font-bold text-center mb-6 text-primary">Convenios para pagos físicos</h3>
            <ul class="space-y-6">
              {paymentMethods[0].items.map((item) => (
                <li class="flex flex-col items-center space-y-2">
                  <img src={item.image} alt={item.name} class="h-12 mb-2" />
                  <p class="text-lg font-bold">{item.name}</p>
                  <p class="text-secondary">Convenio: <span class="font-bold">{item.convenio}</span></p>
                </li>
              ))}
            </ul>
          </div>
      
          <!-- Pagos digitales -->
          <div class="bg-white text-black rounded-lg p-8 shadow-lg">
            <h3 class="text-xl font-bold text-center mb-6 text-primary">Pagos digitales a un clic</h3>
            <ul class="space-y-6">
              {paymentMethods[1].items.map((item) => (
                <li class="flex flex-col items-center space-y-2">
                  <a href={item.href} target="_blank" rel="noopener noreferrer"><img src={item.image} alt={item.name} class="h-24 mb-2" /></a>
                  <p class="text-lg font-bold">{item.name}</p>
                </li>
              ))}
            </ul>
          </div>
        </div>
    
      </section>
      
      
      
</Layout>
